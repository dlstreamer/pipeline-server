{
	"type": "GStreamer",
	"template": ["uridecodebin name=source",
				" ! gvadetect model={models[object_detection][1][network]} model-proc={models[object_detection][1][proc]} name=detection",
				" ! gvaclassify model={models[object_classification][1][network]} model-proc={models[object_classification][1][proc]} name=classification",
				" ! gvametaconvert name=metaconvert ! gvametapublish name=destination",
				" ! appsink name=appsink"],
	"description": "Object Classification Pipeline",
	"parameters": {
		"type": "object",
		"properties": {
			"detection-device": {
				"element": "detection",
				"type": "string"
			},
			"inference-interval": {
				"element": "detection",
				"type": "integer"
			}
		}
	}
}